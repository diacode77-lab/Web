// --- En Program.cs, antes de builder.Build() ---

// Opción A: Redis (recomendado para producción)
builder.Services.AddStackExchangeRedisCache(options =>
{
    // Cadena de conexión a tu servidor Redis
    // Local (ej. Docker): "localhost:6379"
    options.Configuration = builder.Configuration.GetConnectionString("Redis");
    options.InstanceName = "MiApp_";
});

// Registras tu servicio como siempre
builder.Services.AddScoped<SesionService>();
